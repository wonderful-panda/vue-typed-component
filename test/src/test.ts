///<reference types="mocha" />
import * as assert from "power-assert";
import * as Vue from "vue";

describe("vue-typesafe-component", function() {
    it("power-assert works fine", function() {
        assert(1 + 2 === 4);
    });
});
